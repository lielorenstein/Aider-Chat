<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aider_helper">
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_hackathon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_hackathon"
        android:supportsRtl="false"
        android:theme="@style/Theme.AiderHelper">
        <activity android:name=".ui.ChatActivity"></activity>
        <activity android:name=".ui.ListenerActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>